{
  "type": "object",
  "description": "Fired on every key press while typing (before commit).",
  "properties": {
    "draft": {
      "type": "string",
      "description": "The in-progress edit string."
    },
    "value": {
      "type": "number",
      "description": "The draft parsed to a number, or null until it parses to a complete number."
    }
  }
}
