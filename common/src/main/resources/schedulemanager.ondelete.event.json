{
  "type": "object",
  "description": "Fired when the user confirms the two-step Delete button. Persist with system.user.removeSchedule(event.name) and refresh the data.schedules binding.",
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of the schedule to delete."
    }
  }
}
