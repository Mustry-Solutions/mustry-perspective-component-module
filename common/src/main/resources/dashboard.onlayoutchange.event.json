{
  "type": "object",
  "description": "Fired when the operator rearranges the tiles. Persist `layout` and rebind state.layout.",
  "properties": {
    "layout": {
      "type": "array",
      "description": "The new arrangement: [{ id, x, y, w, h }]."
    }
  }
}
