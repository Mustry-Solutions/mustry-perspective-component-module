{
  "type": "object",
  "description": "Fired when the user confirms the two-step Delete button. Persist with system.roster.deleteRoster(event.name) and refresh the data.rosters binding.",
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of the roster to delete."
    }
  }
}
