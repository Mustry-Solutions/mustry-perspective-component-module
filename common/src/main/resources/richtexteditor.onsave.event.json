{
  "type": "object",
  "description": "Fired when the user saves their edits. Persist `content` and rebind data.content.",
  "properties": {
    "content": {
      "type": "string",
      "description": "The document as sanitized (schema-constrained) HTML."
    },
    "plainText": {
      "type": "string",
      "description": "Plain-text mirror of the document."
    },
    "wordCount": {
      "type": "number",
      "description": "Word count of the plain text."
    }
  }
}
