{
    "type": "object",
    "properties": {
        "id": { "type": "string", "description": "The resized event's id." },
        "title": { "type": "string", "description": "The resized event's title." },
        "start": { "type": "string", "description": "The event's start (unchanged)." },
        "end": { "type": "string", "description": "The event's original end, if any." },
        "allDay": { "type": "boolean", "description": "Whether the event is all-day." },
        "newEnd": { "type": "string", "description": "The new end after the resize, ISO datetime." }
    }
}
