{
  "type": "object",
  "additionalProperties": false,
  "required": [],
  "properties": {
    "config": {
      "type": "object",
      "additionalProperties": false,
      "description": "Schedule Manager behaviour and appearance.",
      "default": {
        "locale": "",
        "firstDayOfWeek": "monday",
        "dayStartHour": 0,
        "dayEndHour": 24,
        "editable": true,
        "allowCreate": true,
        "allowDelete": true,
        "snapMinutes": 30
      },
      "properties": {
        "locale": {
          "type": "string",
          "default": "",
          "description": "BCP-47 locale for weekday names (e.g. 'en-US', 'nl-NL'). Empty = browser/session default. Also selects the built-in UI-text language (see config.labels)."
        },
        "firstDayOfWeek": {
          "enum": [
            "monday",
            "sunday"
          ],
          "default": "monday",
          "description": "First day column of the week grid."
        },
        "dayStartHour": {
          "type": "number",
          "default": 0,
          "description": "First hour shown on the week grid's time axis (0-24)."
        },
        "dayEndHour": {
          "type": "number",
          "default": 24,
          "description": "Last hour shown on the week grid's time axis (0-24, > dayStartHour). Availability outside the window is clipped, not lost."
        },
        "editable": {
          "type": "boolean",
          "default": true,
          "description": "The week grid is a paint surface: drag empty space to add availability, drag block edges to resize, click a block to remove; description and the allDays/observeHolidays toggles become editable. All edits are draft-only until Save fires onScheduleSave — handle it and persist (see the ScheduleDemo view). False = viewer."
        },
        "allowCreate": {
          "type": "boolean",
          "default": true,
          "description": "Show the '+ New schedule' flow (a blank draft; Save fires onScheduleSave with isNew: true). Requires editable."
        },
        "allowDelete": {
          "type": "boolean",
          "default": true,
          "description": "Show the two-step Delete button (fires onScheduleDelete). Requires editable."
        },
        "snapMinutes": {
          "type": "number",
          "default": 30,
          "description": "Paint/resize snapping in minutes (5-240). 30 matches typical shift boundaries."
        },
        "labels": {
          "type": "object",
          "additionalProperties": false,
          "description": "Per-key overrides for built-in UI text. Unset keys follow config.locale.",
          "properties": {
            "listHeader": {
              "type": "string",
              "description": "Schedule list heading."
            },
            "noSchedules": {
              "type": "string",
              "description": "Empty-list placeholder."
            },
            "noSelection": {
              "type": "string",
              "description": "Detail placeholder when nothing is selected."
            },
            "allDays": {
              "type": "string",
              "description": "Badge: available 24/7."
            },
            "alternating": {
              "type": "string",
              "description": "Badge: alternating A/B schedule (week A shown)."
            },
            "observesHolidays": {
              "type": "string",
              "description": "Badge: observes gateway holidays."
            },
            "activeNow": {
              "type": "string",
              "description": "Tooltip of the list's active-now dot."
            },
            "save": {
              "type": "string",
              "description": "Save button."
            },
            "discard": {
              "type": "string",
              "description": "Discard-draft button tooltip."
            },
            "unsaved": {
              "type": "string",
              "description": "Unsaved-changes badge."
            },
            "delete": {
              "type": "string",
              "description": "Delete-schedule button."
            },
            "confirmDelete": {
              "type": "string",
              "description": "Second-step delete confirmation."
            },
            "clickToRemove": {
              "type": "string",
              "description": "Block tooltip while editable."
            },
            "description": {
              "type": "string",
              "description": "Description input placeholder."
            },
            "newSchedule": {
              "type": "string",
              "description": "The list rail's create button."
            },
            "name": {
              "type": "string",
              "description": "Name input placeholder."
            },
            "nameRequired": {
              "type": "string",
              "description": "Validation: empty name."
            },
            "nameTaken": {
              "type": "string",
              "description": "Validation: duplicate name."
            },
            "inactive": {
              "type": "string",
              "description": "Preview strip: inactive with no upcoming activation."
            },
            "activeUntil": {
              "type": "string",
              "description": "Preview strip template; {day} and {time} are substituted."
            },
            "inactiveUntil": {
              "type": "string",
              "description": "Preview strip template; {day} and {time} are substituted."
            },
            "moreActions": {
              "type": "string",
              "description": "Accessible name of the per-row ⋯ menu button."
            },
            "duplicate": {
              "type": "string",
              "description": "The row menu's duplicate action (prefills the create flow)."
            }
          }
        }
      }
    },
    "data": {
      "type": "object",
      "additionalProperties": false,
      "description": "The schedules to display. Bind 'schedules' to the gateway's schedule list — typically an expression binding with a script transform over system.user.getSchedules() mapping each model to the flat shape below (see the ScheduleDemo view for the reference script).",
      "default": {
        "schedules": []
      },
      "properties": {
        "schedules": {
          "type": "array",
          "default": [],
          "description": "Array of schedule objects mirroring Ignition's BasicScheduleModel: flat per-day enabled flags + 24h range strings, exactly what system.user.getSchedules() yields and addSchedule/editSchedule accept back. Items stay open — extra fields ride along untouched.",
          "items": {
            "type": "object",
            "properties": {
              "name": {
                "type": "string",
                "description": "Unique schedule name (the selection key)."
              },
              "description": {
                "type": "string",
                "description": "Free-text description."
              },
              "observeHolidays": {
                "type": "boolean",
                "description": "Schedule observes the gateway's holiday list."
              },
              "allDays": {
                "type": "boolean",
                "description": "Available 24/7 — overrides the per-day settings."
              },
              "repeatAlternating": {
                "type": "boolean",
                "description": "Alternating A/B week schedule. This milestone renders week A only."
              },
              "startingAt": {
                "type": "string",
                "description": "ISO date anchoring alternating weeks (informational)."
              },
              "monday": {
                "type": "boolean",
                "description": "Available on Monday."
              },
              "mondayTime": {
                "type": "string",
                "description": "Monday availability, 24h ranges: '8:00-17:00' or '8:00-12:00, 12:30-17:00'. Blank while enabled = all day."
              },
              "tuesday": {
                "type": "boolean",
                "description": "Available on Tuesday."
              },
              "tuesdayTime": {
                "type": "string",
                "description": "Tuesday availability ranges (same format as mondayTime)."
              },
              "wednesday": {
                "type": "boolean",
                "description": "Available on Wednesday."
              },
              "wednesdayTime": {
                "type": "string",
                "description": "Wednesday availability ranges."
              },
              "thursday": {
                "type": "boolean",
                "description": "Available on Thursday."
              },
              "thursdayTime": {
                "type": "string",
                "description": "Thursday availability ranges."
              },
              "friday": {
                "type": "boolean",
                "description": "Available on Friday."
              },
              "fridayTime": {
                "type": "string",
                "description": "Friday availability ranges."
              },
              "saturday": {
                "type": "boolean",
                "description": "Available on Saturday."
              },
              "saturdayTime": {
                "type": "string",
                "description": "Saturday availability ranges."
              },
              "sunday": {
                "type": "boolean",
                "description": "Available on Sunday."
              },
              "sundayTime": {
                "type": "string",
                "description": "Sunday availability ranges."
              }
            }
          }
        }
      }
    },
    "state": {
      "type": "object",
      "additionalProperties": false,
      "description": "Two-way runtime state: the component writes user interactions back here, and writing a value drives it.",
      "default": {
        "selectedSchedule": ""
      },
      "properties": {
        "selectedSchedule": {
          "type": "string",
          "default": "",
          "description": "Name of the selected schedule (two-way): clicking the list writes it back, and setting it selects. When it names no bound schedule, the component selects the first one and writes that back."
        }
      }
    },
    "output": {
      "type": "object",
      "additionalProperties": false,
      "description": "Read-only derived values (the component writes them; do not bind INTO them).",
      "default": {
        "count": 0,
        "isDirty": false
      },
      "properties": {
        "count": {
          "type": "number",
          "default": 0,
          "description": "Number of bound schedules."
        },
        "isDirty": {
          "type": "boolean",
          "default": false,
          "description": "True while the selected schedule's draft differs from the bound data (unsaved edits). Gate navigation or external commit buttons on it."
        },
        "isActiveNow": {
          "type": "boolean",
          "default": false,
          "description": "Whether the SELECTED schedule is active at the current time (re-evaluated every 30s). Alternating schedules evaluate against week A."
        },
        "validationErrors": {
          "type": "array",
          "default": [],
          "description": "Error keys blocking Save ('nameRequired' / 'nameTaken'); empty when the draft is saveable.",
          "items": {
            "type": "string"
          }
        }
      }
    },
    "style": {
      "$ref": "urn:ignition-schema:schemas/style-properties.schema.json",
      "default": {
        "classes": ""
      }
    }
  }
}