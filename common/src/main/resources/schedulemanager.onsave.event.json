{
  "type": "object",
  "description": "Fired when the user presses Save on a dirty draft. Persist event.schedule with system.user.editSchedule (or addSchedule when it does not exist yet) and refresh the data.schedules binding.",
  "properties": {
    "schedule": {
      "type": "object",
      "description": "The full desired schedule as the flat BasicScheduleModel mirror: name, description, observeHolidays, allDays, repeatAlternating, startingAt, and per-day <day>/<day>Time pairs with explicit 24h ranges."
    },
    "isNew": {
      "type": "boolean",
      "description": "True when the schedule does not exist on the gateway yet (create flows arrive in M2; always false today)."
    }
  }
}
