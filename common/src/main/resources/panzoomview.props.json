{
  "type": "object",
  "properties": {
    "config": {
      "type": "object",
      "additionalProperties": false,
      "description": "Set-and-forget configuration: which view to embed and how navigation behaves.",
      "default": {
        "viewPath": "",
        "viewParams": [],
        "contentWidth": 0,
        "contentHeight": 0,
        "minZoom": 0.1,
        "maxZoom": 8,
        "zoomStep": 1.25,
        "wheelZoom": true,
        "doubleClickZoom": true,
        "showControls": true,
        "locale": "",
        "flyToMs": 350,
        "showMinimap": true,
        "showPoiList": true,
        "home": { "x": -1, "y": -1, "zoom": 0 }
      },
      "properties": {
        "viewPath": {
          "type": "string",
          "description": "Path of the Perspective view to embed and navigate."
        },
        "viewParams": {
          "type": "array",
          "description": "Parameters passed into the embedded view: [{name, value}].",
          "default": [],
          "items": {
            "type": "object",
            "properties": {
              "name": { "type": "string", "description": "The view input parameter name." },
              "value": { "description": "The value passed to it (bindable)." }
            }
          }
        },
        "contentWidth": {
          "type": "number",
          "description": "The embedded view's natural width in px — the coordinate space for center/home/pois and the basis for fit. 0 (default) = auto: use the size the embedded view reports.",
          "default": 0
        },
        "contentHeight": {
          "type": "number",
          "description": "The embedded view's natural height in px. 0 (default) = auto.",
          "default": 0
        },
        "minZoom": { "type": "number", "description": "Smallest zoom factor (1 = natural size).", "default": 0.1 },
        "maxZoom": { "type": "number", "description": "Largest zoom factor.", "default": 8 },
        "zoomStep": { "type": "number", "description": "Zoom multiplier per wheel tick / button press (1.05–3).", "default": 1.25 },
        "wheelZoom": { "type": "boolean", "description": "Zoom with the mouse wheel, toward the cursor.", "default": true },
        "doubleClickZoom": { "type": "boolean", "description": "Double-click zooms two steps toward the click point.", "default": true },
        "showControls": { "type": "boolean", "description": "Show the zoom / reset / fit buttons.", "default": true },
        "locale": {
          "type": "string",
          "description": "BCP-47 locale for the built-in control tooltips (en/fr/de/es/nl/it/pt built in). Empty = English."
        },
        "flyToMs": {
          "type": "number",
          "description": "When a script/binding writes state.zoom/center, the viewport animates there over this many ms (0 = snap).",
          "default": 350
        },
        "showMinimap": {
          "type": "boolean",
          "description": "Corner overview with a draggable view rectangle and POI dots. Hides itself while the whole content is visible.",
          "default": true
        },
        "showPoiList": {
          "type": "boolean",
          "description": "'Go to…' dropdown over the viewport when data.pois is non-empty.",
          "default": true
        },
        "home": {
          "type": "object",
          "description": "The reset target (and initial position while state.zoom is unset): a content-coordinate center point and zoom. x/y -1 = content center; zoom 0 = fit.",
          "properties": {
            "x": { "type": "number", "description": "Center x in content coordinates (-1 = content center).", "default": -1 },
            "y": { "type": "number", "description": "Center y in content coordinates (-1 = content center).", "default": -1 },
            "zoom": { "type": "number", "description": "Zoom factor (0 = fit the whole content).", "default": 0 }
          }
        }
      }
    },
    "data": {
      "type": "object",
      "additionalProperties": false,
      "description": "Bound content: named points of interest.",
      "default": { "pois": [] },
      "properties": {
        "pois": {
          "type": "array",
          "description": "Named fly-to targets in content coordinates. Write a name to state.target (or pick it in the 'Go to…' list) to fly there. Flagged POIs get an edge indicator when off-screen and a pulse ring when visible — bind flagged to alarm state.",
          "default": [],
          "items": {
            "type": "object",
            "properties": {
              "name": { "type": "string", "description": "Unique display name (the fly-to key)." },
              "x": { "type": "number", "description": "Content x." },
              "y": { "type": "number", "description": "Content y." },
              "zoom": { "type": "number", "description": "Zoom to fly to (0 = keep the current zoom)." },
              "flagged": { "type": "boolean", "description": "Highlight this POI (edge indicator off-screen, pulse ring on-screen)." }
            }
          }
        }
      }
    },
    "state": {
      "type": "object",
      "additionalProperties": false,
      "description": "Two-way runtime state: navigation writes back here, and writing values drives the viewport (fly-to from bindings/scripts).",
      "default": { "zoom": 0, "center": { "x": 0, "y": 0 }, "target": "" },
      "properties": {
        "zoom": {
          "type": "number",
          "description": "Current zoom factor (two-way). 0/unset = follow config.home.",
          "default": 0
        },
        "center": {
          "type": "object",
          "description": "The content point under the viewport middle (two-way) — write it to fly there.",
          "properties": {
            "x": { "type": "number", "default": 0 },
            "y": { "type": "number", "default": 0 }
          }
        },
        "target": {
          "type": "string",
          "description": "Write a data.pois name to fly to it; the component clears this back to '' after starting the flight (so the same name can be written again)."
        }
      }
    },
    "output": {
      "type": "object",
      "additionalProperties": false,
      "description": "Read-only derived values.",
      "default": { "viewState": "" },
      "properties": {
        "viewState": {
          "type": "string",
          "description": "The embedded view's load state: loading / valid / notFound / error / access-denied."
        }
      }
    },
    "style": {
      "$ref": "urn:ignition-schema:schemas/style-properties.schema.json",
      "default": { "classes": "" }
    }
  }
}
