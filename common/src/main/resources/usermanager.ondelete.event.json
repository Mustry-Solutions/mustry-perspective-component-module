{
  "type": "object",
  "description": "Fired when the user confirms the two-step Delete button. Persist with system.user.removeUser(userSource, event.username) and refresh the data.users binding.",
  "properties": {
    "username": {
      "type": "string",
      "description": "Username of the user to delete."
    }
  }
}
