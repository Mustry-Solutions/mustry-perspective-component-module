{
  "type": "object",
  "description": "Fired when the user saves their edits. Persist `code` and rebind data.code.",
  "properties": {
    "code": {
      "type": "string",
      "description": "The document text."
    },
    "isValid": {
      "type": "boolean",
      "description": "JSON language: whether the saved text parses. Always true otherwise."
    },
    "errorMessage": {
      "type": "string",
      "description": "The JSON parse error ('' when valid)."
    }
  }
}
