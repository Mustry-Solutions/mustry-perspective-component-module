{
    "type": "object",
    "properties": {
        "id": { "type": "string", "description": "The edited event's id (use it to locate the row to update)." },
        "title": { "type": "string", "description": "The updated title." },
        "start": { "type": "string", "description": "Updated start (ISO; date-only when all-day)." },
        "end": { "type": "string", "description": "Updated end (ISO; date-only when all-day)." },
        "allDay": { "type": "boolean", "description": "Whether the event is all-day." },
        "color": { "type": "string", "description": "The chosen colour." },
        "description": { "type": "string", "description": "The updated notes." }
    }
}
