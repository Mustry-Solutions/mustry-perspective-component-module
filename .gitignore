# --- Ignition module generator tool (kept on disk for convenience, not part of this repo) ---
ignition-module-tools/

# --- Local Claude Code tooling ---
# Ignore local Claude state, but DO commit project skills under .claude/skills/<name>.
.claude/*
!.claude/skills
.claude/skills/*
!.claude/skills/verify-component

# --- Gradle ---
.gradle/
build/
**/build/

# --- Node / web front-end ---
node_modules/
**/node_modules/
web/.gradle/

# --- Staged module artifacts for the local dev gateway ---
ops/modules/*.modl

# --- Local dev signing material (NEVER commit keystores/certs/keys) ---
ops/signing/
# Belt-and-suspenders: any stray signing dir + common keystore/cert extensions.
signing/
*.p12
*.jks
*.keystore

# --- IDE / editor ---
.idea/
*.iml
*.ipr
*.iws
.vscode/
# Eclipse/Buildship Java compile output (common/bin, designer/bin, gateway/bin)
bin/

# --- macOS ---
.DS_Store
